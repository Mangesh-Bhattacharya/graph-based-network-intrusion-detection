# Core (needed by src/ and notebooks/01_graph_construction_and_visualization.ipynb)
numpy<2.0               # pinned: some environments hit ABI incompatibilities between
                         # numpy>=2.0 and older compiled wheels (e.g. gensim, a node2vec
                         # dependency); numpy<2.0 is a known-good combination on Windows,
                         # macOS, and Linux alike
pandas>=2.0,<3.0
networkx>=3.0
matplotlib>=3.7

# Notebook execution
jupyter>=1.0
nbformat>=5.9

# Section 2 (Node2Vec embeddings) - notebooks/01_..._kaggle.ipynb
node2vec>=0.4
gensim>=4.3            # installed automatically as a node2vec dependency, pinned here for clarity

# Section 3 (Models: Random Forest, XGBoost, GCN, GraphSAGE, GAT)
scikit-learn>=1.3
xgboost>=2.0
torch>=2.0              # CPU is enough - no GPU/CUDA required for this project's graph sizes.
                         # If this pulls a very large download, install the official CPU-only
                         # build instead: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch-geometric>=2.4
